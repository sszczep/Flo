const boards = require('@server/GloEvents/boards');
const columns = require('@server/GloEvents/columns');
const cards = require('@server/GloEvents/cards');
const comments = require('@server/GloEvents/comments');

module.exports = {
  boards,
  columns,
  cards,
  comments
};
